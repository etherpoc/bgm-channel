# BGMチャンネル制作プロジェクト

作業用BGM(インスト)のYouTubeチャンネルを、AIを活用して制作するプロジェクト。

## 現在のフェーズ

**手動制作 + プロンプトのナレッジ蓄積 + プロンプト改善。**
音楽生成・画像生成・動画作成・編集はすべてユーザーが手動で行う。
このリポジトリ(Claude Code)の役割は次の3つに限定する:
1. 当たったプロンプトを構造化して記録・蓄積する
2. プロンプトの改善案を出す(下記ルールに従う)
3. 蓄積から見えた気づきを言語化する

メディア完成品の生成や動画編集の自動化はまだ行わない。

## 絶対ルール

- 音は必ず `instrumental, no vocals`。
- AI音楽・動画ツールは**有料ティアのみ使用**(商用権が有料に紐づくため)。
- **公開前に全トラックをYouTubeのCopyright checkerに通す**(Lyriaでもゼロリスクではない)。
- アップロード時に**AI開示トグルをオンにする**。
- **量産パターン禁止**。テンプレ的な使い回しはYouTubeの inauthentic content 判定の対象。
  人間のキュレーション(世界観の一貫性・構成・オリジナルのキービジュアル)が差別化の核。

## 使用ツール(確定)

- 音楽: **Google Lyria 3**(入口は Flow Music、将来は Vertex AI / Gemini API)。最大3分→複数生成して長尺ミックス化。
- 静止画: **Midjourney(Basic Plan)**。当面はWeb UIで手動生成。
- 動画(image-to-video): Veo 3.1 等を予定(本格採用時に再検討)。15〜20秒のループ素材を作り編集で繰り返す。
- 編集: DaVinci Resolve(無料)。

## プロンプトの型

```
[genre/style] + [mood] + [tempo/BPM] + [instruments] + [texture/production] + [loopability]
最後に必ず: instrumental, no vocals
+ Negative: 入れたくない要素(急な展開・ボーカル・うるさいドロップ 等)
```

- 英語プロンプト推奨。
- 検証は**一度に1変数だけ変える**(何が効いたか分かるように)。

## ナレッジ運用(記録)

ユーザーからプロンプトと評価を渡されたら:

1. 音楽なら `prompts/music.yaml`、画像なら `prompts/image.yaml` に追記する。
2. 形式は必ず `prompts/_schema.md` に従う。
3. **当たり(winner)だけでなく、却下/惜しかったもの(rejected)も必ず残す。**
4. 蓄積から見えた気づきは `knowledge/insights.md` に書き足す。

## プロンプト改善ルール(Claude Codeが担当)

改善を依頼されたら、以下に従う:

1. **起点を明示する。** どの `id` を元に改善するかを必ず特定する。
2. **一度に1変数だけ変える。** genre / BPM / 楽器 / 質感 / ネガティブ のうち1つ。
   複数同時に変えない(何が効いたか切り分けられなくなるため)。
3. 改善案は **2〜3個に絞り**、それぞれ「何を・なぜ変えたか」の仮説を1行添える。
4. **生成・評価はユーザーが手動で行う。** Claude Codeは案を出すだけで、勝手に winner 判定しない。
5. 評価が返ってきたら**新しい id として記録**する(元エントリは残す)。
   `parent` で起点を、`changed` で変更点を1語で紐づける。
6. 確定した気づきは `knowledge/insights.md` に反映する。

> 注意: 改善方向は「無難な平均」に寄りがち。合否判定(rating)と
> 「何が惜しいか」の言語化は必ずユーザーが握る。Claude Codeは案出しのみ。

## メディア完成品の置き場(Git管理しない)

音源・画像・動画は重いバイナリのため**リポジトリに入れない**。
リポジトリ外の `bgm-assets/`(または GCS / Google Drive)にリリース単位で置く。
`.gitignore` でメディア類はコミット対象から除外済み。

リポジトリ側には「どのプロンプト群がどのリリース・どのYouTube動画になったか」の
対応表だけを `docs/releases.md` に残し、再現性を担保する。

## 参照

- 全体方針・決定事項の詳細: `docs/production-report.md`
- プロンプト記録フォーマット: `prompts/_schema.md`
- 逆算メモ・世界観の言語化: `knowledge/insights.md`
- リリース対応表: `docs/releases.md`
