# skills(将来用・現在は空)

このディレクトリは将来のClaude Code Skill用に予約。**今は何も置かない。**

記録を手作業で何件か行い、`prompts/_schema.md` のフォーマットに過不足がないと
確認できてから、以下のようなSkillを追加する想定:

- `/log-prompt` … 対話で渡されたプロンプトと評価を `_schema.md` 形式で
  `prompts/*.yaml` に追記する記録係。

最初からSkill化すると項目の過不足に後で気づいて作り直しになるため、
フォーマットが固まるまでは手動で運用する。
